colors = ["green", "orange", "yellow"]

# Call enumerate() to access indexes and elements.
# ... Unpack each tuple returned from enumerate().
for i, color in enumerate(colors):
    print("INDEX:", i)
    print("COLOR:", color)
